#include <string>
#include <iostream>
using namespace std;

int main() {
    // account x("haha", 0);
    // p(x);
    // unsigned long int x, y;
    // x = ULONG_MAX-1;
    // y = -1;
    cout<<ULONG_MAX<<endl;
    cout<<LONG_MAX*int(2)<<endl;
    // x.deposit(ULONG_MAX);
    // x.deposit(-1);
    // x.print();
    // x.deposit(1);
}
